# Journal2Ct / Nikita

Высокопроизводительный сервис парсинга и экспорта журналов событий 1С:Предприятие.
Поддерживает экспорт в **ClickHouse** (для аналитики и длительного хранения), **Solr** (для полнотекстового поиска), с опциональной буферизацией через **Redis** для надежности.

> 🚀 **Быстрый старт для Linux**: Смотрите [QUICKSTART.md](../QUICKSTART.md) для установки за 5 команд!

## Возможности

*   **Высокая производительность**: Многопоточный парсинг логов 1С с автоматическим распределением нагрузки.
*   **Поддержка форматов**: Работает как со старым текстовым форматом (`.lgf`), так и с новым SQLite (`.lgd`).
*   **Гибкое хранилище**:
    *   **ClickHouse** (включен по умолчанию): Колоночное хранение для аналитических запросов, автосоздание БД и таблиц, максимальное сжатие данных.
    *   **Solr** (опционально): Полнотекстовый поиск по содержимому логов.
    *   **Redis** (опционально): Очередь для буферизации данных при недоступности основных хранилищ.
*   **Автоматическое обнаружение баз**: Поддержка автодетекта баз 1С из кластеров + возможность ручного указания путей.
*   **Веб-панель мониторинга**: Встроенный HTTP-сервер с визуализацией статистики, статуса подключений и ошибок в реальном времени.
*   **JSON API**: REST API для интеграции с системами мониторинга (Prometheus, Zabbix, Grafana).
*   **Кроссплатформенность**: Работает как служба **Windows** или демон **Linux** (systemd).
*   **Конфигурация через ENV**: Все настройки через переменные окружения (файл `.env`). Безопасное хранение секретов.

## Требования

*   **Python 3.10+**
*   **ClickHouse** (рекомендуется, включен по умолчанию)
*   **Redis** (опционально, для буферизации)
*   **Solr** (опционально, для полнотекстового поиска)
*   **Журналы сервера 1С:Предприятие** (доступные через файловую систему)

## Установка

### Рекомендуемый путь установки для Linux

```bash
# 1. Клонируйте репозиторий в /opt
sudo git clone https://github.com/your-repo/Nikita.git /opt/Nikita
cd /opt/Nikita

# 2. Создайте виртуальное окружение
sudo python3 -m venv venv
sudo venv/bin/pip install -r requirments.lin

# 3. Настройте конфигурацию
sudo cp env.example .env
sudo nano .env  # Отредактируйте под вашу конфигурацию

# 4. Установите службу через симлинк
sudo ln -s /opt/Nikita/journal2ct.service /etc/systemd/system/journal2ct.service
sudo systemctl daemon-reload
sudo systemctl enable journal2ct
sudo systemctl start journal2ct

# 5. Проверьте статус
sudo systemctl status journal2ct
```

### Альтернативная установка (произвольный путь)

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/your-repo/Nikita.git
    cd Nikita
    ```

2.  Создайте виртуальное окружение и установите зависимости:
    ```bash
    python -m venv venv
    # Linux
    source venv/bin/activate
    # Windows
    .\venv\Scripts\activate
    
    pip install -r requirments.lin  # Linux
    # или
    pip install -r requirments.win  # Windows
    ```

## Конфигурация

Приложение считывает настройки из переменных окружения через файл `.env`.

### 1. Создание файла конфигурации

Скопируйте пример конфигурации:
```bash
cp env.example .env
```

### 2. Настройка баз данных 1С

У вас есть три варианта настройки баз данных:

#### Вариант А: Автоматический детект (рекомендуется)

Укажите корневой путь к базам 1С — приложение автоматически найдёт все базы в кластерах:

```ini
# 1C Settings - автоматический детект
C1_SRVINFO_PATH=/home/usr1cv8/.1cv8/1C/1cv8
```

#### Вариант Б: Ручное указание баз

Укажите каждую базу вручную (используется префикс `IBASE_N`, где N = 0, 1, 2...):

```ini
# База #0
IBASE_0=PROD_ZUP_LOCAL
IBASE_0_JR=/home/usr1cv8/.1cv8/1C/1cv8/reg_1541/uuid/1Cv8Log
IBASE_0_FORMAT=lgf  # lgf (старый формат) или lgd (SQLite)

# База #1
IBASE_1=ANOTHER_BASE
IBASE_1_JR=/path/to/another/log
IBASE_1_FORMAT=lgd
```

#### Вариант В: Комбинированный

Можно использовать оба способа одновременно:

```ini
# Автодетект
C1_SRVINFO_PATH=/home/usr1cv8/.1cv8/1C/1cv8

# Дополнительная база вне стандартного пути
IBASE_0=EXTERNAL_BASE
IBASE_0_JR=/mnt/external/base/1Cv8Log
IBASE_0_FORMAT=lgf
```

### 3. Настройка хранилищ данных

#### ClickHouse (включено по умолчанию)

```ini
CLICKHOUSE_ENABLED=true
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=zhr1c
```

**Важно:** База данных и таблицы создаются автоматически при первом запуске. Используется максимальное сжатие для экономии места.

#### Redis (опционально, для буферизации)

```ini
REDIS_ENABLED=false
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
```

#### Solr (опционально, для полнотекстового поиска)

```ini
SOLR_ENABLED=false
SOLR_HOST=127.0.0.1
SOLR_PORT=8983
```

### 4. HTTP-сервер мониторинга

```ini
HTTP_LISTEN_INTERFACE=0.0.0.0
HTTP_LISTEN_PORT=8984
```

После запуска веб-панель доступна по адресу: `http://localhost:8984/`

### 5. Отладка

```ini
DEBUG_PARSER=false
```

**Примечание:** Флаг `DEBUG_ENABLED` управляется непосредственно из веб-интерфейса с помощью галочки "🐛 Отладка".

Логи сохраняются в каталоге `debug/`.

## Использование / Развертывание

### Консольный режим (Отладка)

Запуск напрямую в консоли для просмотра логов и отладочной информации:

```bash
python Nikita.py console
```

### Linux (Systemd)

#### Быстрая установка (рекомендуется)

В репозитории уже есть готовый файл `journal2ct.service`. Просто создайте симлинк:

```bash
# Создаём симлинк на unit-файл
sudo ln -s /opt/Nikita/journal2ct.service /etc/systemd/system/journal2ct.service

# Перезагружаем конфигурацию systemd
sudo systemctl daemon-reload

# Включаем автозапуск и запускаем службу
sudo systemctl enable journal2ct
sudo systemctl start journal2ct

# Проверяем статус
sudo systemctl status journal2ct
```

**Важно:** Убедитесь, что:
- Путь установки соответствует `/opt/Nikita` (или отредактируйте `WorkingDirectory` в `journal2ct.service`)
- Пользователь `usr1cv8` существует и имеет доступ к каталогам журналов 1С
- Файл `.env` настроен корректно

#### Ручная установка (альтернатива)

Если вы устанавливаете Nikita в другой каталог или используете другого пользователя:

1.  Скопируйте и отредактируйте unit-файл:

    ```bash
    sudo cp /path/to/Nikita/journal2ct.service /etc/systemd/system/
    sudo nano /etc/systemd/system/journal2ct.service
    ```
    
    Измените параметры `User`, `Group` и `WorkingDirectory` под вашу конфигурацию.

2.  Активируйте и запустите:

    ```bash
    sudo systemctl daemon-reload
    sudo systemctl enable journal2ct
    sudo systemctl start journal2ct
    ```

### Windows Service

1.  Установка службы (требуются права Администратора):
    ```powershell
    python Nikita.py install
    ```

2.  Запуск службы:
    ```powershell
    python Nikita.py start
    ```

3.  Удаление службы:
    ```powershell
    python Nikita.py remove
    ```

## Архитектура

```
┌─────────────┐
│  1C Journals│  (lgf/lgd файлы)
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────────────┐
│  Parser Threads (многопоточный парсинг) │
└──────┬──────────────────────────┬───────┘
       │                          │
       │ Redis включен?           │ Redis выключен?
       ↓ ДА                       ↓ НЕТ
┌──────────────┐           ┌──────────────┐
│ Redis Queue  │           │ Прямая       │
│ (буферизация)│           │ отправка     │
└──────┬───────┘           └──────┬───────┘
       │                          │
       ↓                          │
┌──────────────┐                  │
│ Sender Thread│──────────────────┘
└──────┬───────┘
       │
       ├─────────────────┬──────────────────┐
       ↓                 ↓                  ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  ClickHouse  │  │     Solr     │  │     Redis    │
│  (включен)   │  │ (опционально)│  │ (статистика) │
└──────────────┘  └──────────────┘  └──────────────┘
       │
       ↓
┌──────────────────────────────────┐
│  HTTP Server (CherryPy)          │
│  - Веб-панель мониторинга        │
│  - JSON API для внешних систем   │
└──────────────────────────────────┘
```

### Компоненты системы:

1.  **Parser Threads (Потоки парсера)**: 
    - Мониторят каталоги логов 1С (`.lgf`/`.lgd`)
    - При появлении новых данных парсят их и формируют батчи
    - Автоматически обнаруживают новые/удалённые базы
    - Поддерживают оба формата журналов

2.  **Redis Queue (опционально)**: 
    - Персистентный буфер для данных
    - Если ClickHouse/Solr недоступны, очередь растет, но данные сохраняются
    - Гарантирует отсутствие потери данных

3.  **Sender Thread (Поток отправителя)**: 
    - Забирает данные из очереди или напрямую от парсера
    - Пакетная отправка в ClickHouse и Solr
    - Автоматическое создание БД и таблиц
    - Учет статистики и ошибок

4.  **ClickHouse**: 
    - Основное хранилище (включено по умолчанию)
    - Автосоздание базы данных и таблиц при первом запуске
    - Максимальное сжатие данных (кодек ZSTD)
    - Отдельная таблица для каждой базы 1С

5.  **HTTP Server**: 
    - Веб-панель мониторинга на `http://localhost:8984/`
    - JSON API на `/stats_api` для интеграции с мониторингом
    - Отображение статистики, статуса подключений, последних ошибок

## Устранение неполадок

### Логи и отладка

*   **Каталог логов**: `debug/` в корне проекта
*   **Формат логов**: `Nikita.<PID>.log`
*   **Просмотр логов в реальном времени**:
    ```bash
    tail -f debug/Nikita.*.log
    ```
*   **Фильтр только ошибок**:
    ```bash
    tail -f debug/Nikita.*.log | grep "✗"
    ```

### Веб-панель мониторинга

Откройте браузер и перейдите на `http://localhost:8984/` для просмотра:
- Статуса подключения к ClickHouse/Solr/Redis (🟢 зелёный = OK, 🔴 красный = ошибка)
- Счётчиков отправленных записей и ошибок
- Списка последних ошибок с временными метками
- Списка обрабатываемых баз 1С и их статуса

### JSON API для мониторинга

```bash
curl http://localhost:8984/stats_api
```

Возвращает JSON со всей статистикой работы службы. Можно интегрировать с Prometheus, Zabbix, Grafana.

### Типичные проблемы

#### 1. Не найдены базы данных 1С

**Симптомы**: В логах `⚠ ВНИМАНИЕ: Не найдено ни одной базы данных 1С!`

**Решение**:
- Укажите `C1_SRVINFO_PATH` в `.env` для автодетекта
- Или укажите базы вручную через `IBASE_0`, `IBASE_0_JR`, `IBASE_0_FORMAT`
- Проверьте права доступа к каталогам журналов 1С

#### 2. ClickHouse недоступен

**Симптомы**: 🔴 на веб-панели, ошибки в логах `✗ ClickHouse: Ошибка подключения`

**Решение**:
- Убедитесь, что ClickHouse запущен: `systemctl status clickhouse-server`
- Проверьте настройки подключения в `.env`
- Проверьте права пользователя БД

#### 3. Права доступа к журналам 1С

**Симптомы**: База обнаружена, но записи не парсятся

**Решение**:
```bash
# Добавьте пользователя в группу usr1cv8
sudo usermod -a -G usr1cv8 $(whoami)

# Или используйте скрипт для настройки прав (Linux)
./scripts/share.1cdata.sh
```

#### 4. Redis не запускается (если включен)

**Решение**: Убедитесь, что Redis установлен и запущен:
```bash
sudo systemctl start redis
sudo systemctl enable redis
```

Или отключите Redis в `.env`:
```ini
REDIS_ENABLED=false
```

### Проверка статуса службы

**Linux (systemd)**:
```bash
# Статус службы
sudo systemctl status journal2ct

# Просмотр логов в реальном времени
sudo journalctl -u journal2ct -f

# Последние 100 строк логов
sudo journalctl -u journal2ct -n 100

# Логи с определённого времени
sudo journalctl -u journal2ct --since "10 minutes ago"

# Перезапуск службы
sudo systemctl restart journal2ct

# Остановка службы
sudo systemctl stop journal2ct

# Отключение автозапуска
sudo systemctl disable journal2ct
```

**Windows**:
```powershell
sc query Journal2Ct
Get-EventLog -LogName Application -Source Journal2Ct -Newest 10
```
