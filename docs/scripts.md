# Описание скриптов проекта Nikita

В каталоге `scripts/` находятся вспомогательные скрипты для сборки, установки и управления сервисом Nikita.

---

## 📦 Сборочные скрипты

### Компиляция проекта

#### `build.ps1`
**Платформа**: Windows (PowerShell)  
**Назначение**: Основная сборка приложения с помощью PyInstaller, включая автоматическую загрузку Java и Solr.

**Использование**:
```powershell
.\scripts\build.ps1 [-Optimize] [-NoTest] [-OutputName <name>]
```

**Параметры**:
- `-Optimize`: Включает оптимизацию и компиляцию .pyd файлов
- `-NoTest`: Пропускает тестирование собранного приложения
- `-OutputName`: Имя выходного каталога (по умолчанию: "Nikita")

#### `__CI.Build.cmd`
**Платформа**: Windows  
**Назначение**: Полная сборка проекта для CI/CD пайплайнов + создание инсталлятора.

**Использование**:
```cmd
scripts\__CI.Build.cmd
```

#### `download-deps.ps1`
**Платформа**: Windows (PowerShell)  
**Назначение**: Автоматическая загрузка и установка Java 17 и Apache Solr 9.x.

**Использование**:
```powershell
.\scripts\download-deps.ps1 [-Force]
```

**Параметры**:
- `-Force`: Перезагрузить зависимости даже если они уже существуют

### Компиляция в .pyd (защита кода)

#### `b.2pyd.cmd`
**Платформа**: Windows  
**Назначение**: Обёртка для запуска `b.compiles2pyd.py` — компиляция Python-модулей в бинарные `.pyd` файлы.

**Использование**:
```cmd
scripts\b.2pyd.cmd
```

#### `b.compiles2pyd.py`
**Платформа**: Кроссплатформенный (Python)  
**Назначение**: Скрипт для компиляции Python-модулей в бинарные файлы через Cython для защиты исходного кода и оптимизации производительности.

**Использование**:
```bash
python scripts/b.compiles2pyd.py
```

### Создание инсталлятора

#### `!c.build.win.distr.cmd`
**Платформа**: Windows  
**Назначение**: Полный цикл создания Windows-дистрибутива (компиляция + создание установщика).

**Использование**:
```cmd
scripts\!c.build.win.distr.cmd
```

**Что делает**:
1. Компилирует проект через PowerShell скрипт `build.ps1`
2. Создаёт Windows-инсталлятор через NSIS (`c.installer.nsi`)

#### `c.installer.nsi`
**Платформа**: Windows  
**Назначение**: Сценарий для NSIS (Nullsoft Scriptable Install System) — создание установщика Windows.

**Использование**: Автоматически используется скриптом `!c.build.win.distr.cmd`.

**Ручной запуск** (если установлен NSIS):
```cmd
makensis scripts\c.installer.nsi
```

### Очистка проекта

#### `__Clean.cmd`
**Платформа**: Windows  
**Назначение**: Очистка временных файлов и каталогов сборки (`build/`, `dist/`, `*.pyc`, `__pycache__`).

**Использование**:
```cmd
scripts\__Clean.cmd
```

**Что удаляется**:
- `build/` — промежуточные файлы компиляции
- `dist/` — собранные дистрибутивы
- `*.pyc` — скомпилированные Python-файлы
- `__pycache__/` — кэш Python
- `*.pyd` — бинарные модули (если есть)

---

## 🔧 Управление сервисом (Windows)

Эти скрипты используются для управления установленной Windows-службой или для отладки скомпилированного дистрибутива.

#### `run.dist.install.cmd`
**Назначение**: Установка службы Nikita в систему Windows.

**Использование** (требуются права Администратора):
```cmd
scripts\run.dist.install.cmd
```

**Что происходит**:
- Регистрация службы `Nikita` в Windows Service Manager
- Настройка автозапуска (опционально)

#### `run.dist.console.cmd`
**Назначение**: Запуск скомпилированной версии в консольном режиме (для отладки).

**Использование**:
```cmd
scripts\run.dist.console.cmd
```

**Применение**: Отладка дистрибутива без установки как службы. Все логи выводятся в консоль.

#### `run.dist.start.cmd`
**Назначение**: Запуск установленной службы.

**Использование** (требуются права Администратора):
```cmd
scripts\run.dist.start.cmd
```

Эквивалентно:
```cmd
net start Nikita
```

#### `run.dist.stop.cmd`
**Назначение**: Остановка работающей службы.

**Использование** (требуются права Администратора):
```cmd
scripts\run.dist.stop.cmd
```

Эквивалентно:
```cmd
net stop Nikita
```

#### `run.dist.remove.cmd`
**Назначение**: Удаление службы из системы.

**Использование** (требуются права Администратора):
```cmd
scripts\run.dist.remove.cmd
```

**Что происходит**:
- Остановка службы (если запущена)
- Удаление из Windows Service Manager

---

## 🛠 Утилиты

#### `add.firewall.rule.cmd`
**Платформа**: Windows  
**Назначение**: Добавление правил в Windows Firewall для работы веб-сервера мониторинга.

**Использование** (требуются права Администратора):
```cmd
scripts\add.firewall.rule.cmd
```

**Что делает**:
- Открывает порт `8984` (HTTP-сервер мониторинга) в Windows Firewall
- Добавляет исключение для `Nikita.exe`

**Настройка**: Если вы изменили порт в `.env` (`HTTP_LISTEN_PORT`), отредактируйте скрипт соответствующим образом.

#### `install.python.modules.cmd`
**Платформа**: Windows  
**Назначение**: Установка всех необходимых Python-библиотек из файлов зависимостей.

**Использование**:
```cmd
scripts\install.python.modules.cmd
```

**Что происходит**:
```cmd
pip install -r requirments.win
```

**Примечание**: Убедитесь, что виртуальное окружение активировано перед запуском.

#### `share.1cdata.sh`
**Платформа**: Linux  
**Назначение**: Настройка прав доступа и шаринга данных 1С для службы Nikita.

**Использование** (требуются права root):
```bash
sudo ./scripts/share.1cdata.sh
```

**Что делает**:
- Добавляет текущего пользователя в группу `usr1cv8` (стандартная группа для файлов 1С)
- Настраивает права доступа к журналам регистрации 1С
- Устанавливает ACL (Access Control Lists) для чтения журналов

**Применение**: Необходимо для корректной работы службы на Linux, если она запускается от пользователя, отличного от `usr1cv8`.

#### `run-tests.ps1`
**Платформа**: Windows (PowerShell)  
**Назначение**: Запуск тестов проекта с автоматическим созданием виртуального окружения.

**Использование**:
```powershell
.\scripts\run-tests.ps1 [-Verbose] [-CreateVenv] [-VenvPath <path>]
```

**Параметры**:
- `-Verbose`: Подробный вывод результатов тестов
- `-CreateVenv`: Создать новое виртуальное окружение
- `-VenvPath`: Путь к виртуальному окружению (по умолчанию: "venv")

#### `!.test.cmd`
**Платформа**: Windows  
**Назначение**: Запуск тестов проекта (устаревший, используйте run-tests.ps1).

**Использование**:
```cmd
scripts\!.test.cmd
```

---

## 📝 Примечания

### Общие правила

1. **Рабочая директория**: Все скрипты автоматически переключают рабочую директорию на корень проекта (`/home/wondermr/repos/Nikita` или `C:\path\to\Nikita`).

2. **Права доступа**: 
   - **Windows**: Скрипты управления службой требуют прав Администратора.
   - **Linux**: Скрипты, работающие с системными файлами, требуют `sudo`.

3. **Виртуальное окружение**: Перед запуском скриптов установки зависимостей или компиляции убедитесь, что виртуальное окружение активировано:
   ```bash
   # Linux
   source venv/bin/activate
   
   # Windows
   .\venv\Scripts\activate
   ```

### Рекомендуемый порядок действий

#### Для разработки (Windows):
```cmd
1. scripts\install.python.modules.cmd     # Установка зависимостей
2. .\scripts\download-deps.ps1             # Загрузка Java/Solr (если нужно)
3. .\scripts\build.ps1                     # Компиляция приложения
4. .\scripts\run-tests.ps1                 # Запуск тестов
5. scripts\run.dist.console.cmd           # Тестирование в консоли
6. scripts\run.dist.install.cmd           # Установка службы
7. scripts\run.dist.start.cmd             # Запуск службы
```

#### Для разработки (Linux):
```bash
1. pip install -r requirements.lin               # Установка зависимостей
2. sudo ./scripts/share.1cdata.sh               # Настройка прав доступа к 1С
3. python Nikita.py console                     # Тестирование в консоли
4. sudo systemctl enable Nikita             # Установка службы
5. sudo systemctl start Nikita              # Запуск службы
```

#### Для создания дистрибутива (Windows):
```cmd
1. scripts\__Clean.cmd                     # Очистка
2. .\scripts\download-deps.ps1             # Загрузка зависимостей
3. scripts\!c.build.win.distr.cmd         # Полная сборка + установщик
```

#### Быстрый старт для CI/CD:
```cmd
1. scripts\install.python.modules.cmd     # Установка зависимостей
2. scripts\__CI.Build.cmd                 # Полная сборка + инсталлятор
```

### Структура после компиляции

После успешной компиляции в каталоге `dist/` появится:
```
dist/
├── Nikita.exe                 # Основной исполняемый файл
├── dlls/                      # Необходимые DLL библиотеки
│   ├── libcrypto-1_1-x64.dll
│   ├── libssl-1_1-x64.dll
│   └── sqlite3.dll
├── licenses/                  # Лицензии используемых компонентов
└── setup.exe                  # Установщик (если запускался !c.build.win.distr.cmd)
```

---

## 📁 Структура скриптов

```
scripts/
├── common.ps1              # Общие функции PowerShell
├── build.ps1               # Основная сборка приложения
├── download-deps.ps1       # Автозагрузка Java/Solr
├── run-tests.ps1           # Запуск тестов Windows
├── __CI.Build.cmd          # CI точка входа (сборка + инсталлятор)
├── __Clean.cmd             # Очистка временных файлов
├── !c.build.win.distr.cmd  # Полная сборка дистрибутива
├── !.test.cmd              # Запуск тестов (устаревший)
├── c.installer.nsi         # NSIS скрипт инсталлятора
├── b.2pyd.cmd              # Компиляция в .pyd
├── b.compiles2pyd.py       # Cython компилятор
├── install.python.modules.cmd # Установка зависимостей
├── run.dist.*.cmd          # Управление Windows-службой
├── add.firewall.rule.cmd   # Настройка firewall
└── share.1cdata.sh         # Настройка прав Linux
```

---

## 🔍 Troubleshooting

### Ошибка компиляции на Windows

**Проблема**: `PyInstaller not found`

**Решение**:
```cmd
pip install pyinstaller
```

### Ошибка создания установщика

**Проблема**: `makensis: command not found`

**Решение**: Установите NSIS с официального сайта: https://nsis.sourceforge.io/

### Ошибка прав доступа на Linux

**Проблема**: `Permission denied` при запуске `.sh` скриптов

**Решение**:
```bash
chmod +x scripts/*.sh
```

### Служба не запускается на Windows

**Проблема**: `Error 1053: The service did not respond to the start or control request in a timely fashion`

**Решение**:
1. Проверьте логи в каталоге `debug/`
2. Убедитесь, что файл `.env` существует
3. Запустите сначала в консольном режиме: `scripts\run.dist.console.cmd`

---

*Обновлено: 2025-12-10*
*Проект: Nikita / Nikita*
