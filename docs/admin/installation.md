# Установка Nikita

Полное руководство по установке службы Nikita на Linux и Windows.

---

## Содержание

- [Системные требования](#системные-требования)
- [Установка на Linux](#установка-на-linux)
- [Установка на Windows](#установка-на-windows)
- [Первичная настройка](#первичная-настройка)
- [Проверка установки](#проверка-установки)
- [Обновление](#обновление)
- [Удаление](#удаление)

---

## Системные требования

### Минимальные требования

- **Процессор:** 2 ядра (рекомендуется 4+)
- **Оперативная память:** 2 GB (рекомендуется 4+ GB)
- **Дисковое пространство:** 500 MB для приложения + место для логов
- **Python:** 3.10 или выше
- **Доступ:** к каталогам журналов регистрации 1С:Предприятие

### Рекомендуемые компоненты

- **ClickHouse** 21.3+ — для хранения данных (включен по умолчанию)
- **Redis** 6.0+ — для буферизации (опционально)
- **Apache Solr** 8.x/9.x — для полнотекстового поиска (опционально)

### Поддерживаемые платформы

**Linux:**
- Ubuntu 20.04, 22.04, 24.04
- Debian 11, 12
- CentOS/RHEL 8, 9
- Rocky Linux 8, 9
- Astra Linux (SE, CE)

**Windows:**
- Windows 10 (1809+)
- Windows 11
- Windows Server 2016, 2019, 2022

---

## Установка на Linux

### Способ 1: Установка из исходников (рекомендуется)

#### Шаг 1: Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git
```

**CentOS/RHEL:**
```bash
sudo yum install -y python3 python3-pip git
```

#### Шаг 2: Клонирование репозитория

```bash
# Клонируем в /opt/Nikita
sudo git clone https://github.com/your-org/Nikita.git /opt/Nikita
cd /opt/Nikita
```

#### Шаг 3: Создание виртуального окружения

```bash
sudo python3 -m venv venv
sudo venv/bin/pip install --upgrade pip
sudo venv/bin/pip install -r requirements.lin
```

#### Шаг 4: Настройка прав доступа

```bash
# Передаём владение пользователю 1С (обычно usr1cv8:grp1cv8)
sudo chown -R usr1cv8:grp1cv8 /opt/Nikita

# Или добавляем текущего пользователя в группу usr1cv8
sudo usermod -a -G usr1cv8 $USER

# Применяем изменения группы
newgrp usr1cv8
```

#### Шаг 5: Конфигурация

```bash
sudo cp env.example .env
sudo nano .env
```

Минимальная конфигурация:
```ini
# Путь к базам 1С для автодетекта
C1_SRVINFO_PATH=/home/usr1cv8/.1cv8/1C/1cv8

# ClickHouse
CLICKHOUSE_ENABLED=true
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_DATABASE=zhr1c

# Веб-сервер
HTTP_LISTEN_PORT=8984
```

#### Шаг 6: Установка systemd service

```bash
# Создаём симлинк на unit-файл
sudo ln -s /opt/Nikita/Nikita.service /etc/systemd/system/Nikita.service

# Перезагружаем конфигурацию systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable Nikita

# Запускаем службу
sudo systemctl start Nikita
```

#### Шаг 7: Проверка

```bash
# Статус службы
sudo systemctl status Nikita

# Логи в реальном времени
sudo journalctl -u Nikita -f
```

### Способ 2: Запуск без установки службы (для разработки)

```bash
cd /opt/Nikita
source venv/bin/activate
python Nikita.py console
```

Этот режим полезен для отладки — все логи выводятся в консоль.

---

## Установка на Windows

### Способ 1: Установка из дистрибутива (рекомендуется)

#### Шаг 1: Сборка инсталлятора

Так как готовый инсталлятор не предоставляется, его необходимо собрать из исходников:

1. Следуйте инструкции в [BUILD.md](../../BUILD.md).
2. После сборки инсталлятор будет находиться в папке `dist/` (например, `Nikita.setup.2025.12.12.exe`).

#### Шаг 2: Запуск инсталлятора

1. Запустите собранный `Nikita.setup.ДАТА.exe` **от имени Администратора**
2. Выберите каталог установки (по умолчанию: `C:\Program Files\Nikita`)
3. Дождитесь завершения установки

Инсталлятор автоматически:
- Распакует все файлы
- Установит Java JDK 17 (если не установлена)
- Установит Apache Solr (опционально)
- Создаст ярлыки в меню Пуск

#### Шаг 3: Конфигурация

Откройте файл `.env` в каталоге установки:
```powershell
notepad "C:\Program Files\Nikita\.env"
```

Минимальная конфигурация:
```ini
# Путь к базам 1С (Windows)
C1_SRVINFO_PATH=C:\Users\usr1cv8\AppData\Roaming\1C\1cv8

# ClickHouse
CLICKHOUSE_ENABLED=true
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000

# Веб-сервер
HTTP_LISTEN_PORT=8984
```

#### Шаг 4: Установка и запуск службы

Откройте **PowerShell от имени Администратора**:
```powershell
cd "C:\Program Files\Nikita"

# Установка службы
.\Nikita.exe install

# Запуск службы
.\Nikita.exe start
```

Или через графический интерфейс:
1. Откройте `services.msc`
2. Найдите службу "Nikita"
3. Правой кнопкой → "Запустить"

#### Шаг 5: Проверка

Откройте браузер: `http://localhost:8984/`

### Способ 2: Из исходников (для разработки)

**Важно:** под Windows запуск в виде службы рассчитан на **`Nikita.exe`**.
Если вы взяли исходники — **сначала соберите** дистрибутив (см. [BUILD.md](../../BUILD.md)), затем устанавливайте/запускайте службу.
Запуск `python Nikita.py console` оставляйте для отладки.

#### Шаг 1: Установка Python

Скачайте и установите Python 3.10+ с [python.org](https://www.python.org/downloads/)

**Важно:** При установке отметьте галочку "Add Python to PATH"

#### Шаг 2: Установка Git

Скачайте и установите Git с [git-scm.com](https://git-scm.com/download/win)

#### Шаг 3: Клонирование репозитория

```powershell
git clone https://github.com/your-org/Nikita.git
cd Nikita
```

#### Шаг 4: Создание виртуального окружения

```powershell
python -m venv venv
.\venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.win
```

#### Шаг 5: Конфигурация

```powershell
copy env.example .env
notepad .env
```

#### Шаг 6: Установка службы

Откройте **PowerShell от имени Администратора**:
```powershell
python Nikita.py install
python Nikita.py start
```

#### Шаг 7: Запуск в консольном режиме (для разработки)

```powershell
python Nikita.py console
```

---

## Первичная настройка

### 1. Настройка ClickHouse

Если ClickHouse не установлен:

**Ubuntu/Debian:**
```bash
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 8919F6BD2B48D754
echo "deb https://packages.clickhouse.com/deb stable main" | sudo tee /etc/apt/sources.list.d/clickhouse.list
sudo apt update
sudo apt install -y clickhouse-server clickhouse-client
sudo systemctl start clickhouse-server
sudo systemctl enable clickhouse-server
```

**Windows:**
- Скачайте с [clickhouse.com](https://clickhouse.com/docs/en/install)
- Или используйте Docker: `docker run -d -p 9000:9000 clickhouse/clickhouse-server`

### 2. Настройка прав доступа к журналам 1С (Linux)

```bash
# Добавьте пользователя службы в группу 1С
sudo usermod -a -G usr1cv8 usr1cv8

# Или используйте готовый скрипт
sudo ./scripts/share.1cdata.sh
```

### 3. Настройка Firewall

**Linux (Ubuntu/Debian):**
```bash
sudo ufw allow 8984/tcp
```

**Linux (CentOS/RHEL):**
```bash
sudo firewall-cmd --add-port=8984/tcp --permanent
sudo firewall-cmd --reload
```

**Windows:**
```powershell
# Выполните от Администратора
netsh advfirewall firewall add rule name="Nikita Web" dir=in action=allow protocol=TCP localport=8984
```

### 4. Проверка конфигурации баз 1С

Запустите в консольном режиме для проверки:

**Linux:**
```bash
/opt/Nikita/venv/bin/python /opt/Nikita/Nikita.py console
```

**Windows:**
```powershell
python Nikita.py console
```

Вы должны увидеть:
```
✓ Найдено баз: 2
✓ База 1: PROD_ZUP (/path/to/1Cv8Log)
✓ База 2: TEST_UT11 (/path/to/1Cv8Log)
```

Если базы не найдены — проверьте `C1_SRVINFO_PATH` в `.env`.

---

## Проверка установки

### 1. Проверка статуса службы

**Linux:**
```bash
sudo systemctl status Nikita
```

Ожидаемый вывод:
```
● Nikita.service - Nikita Service
   Loaded: loaded (/etc/systemd/system/Nikita.service; enabled)
   Active: active (running) since ...
```

**Windows:**
```powershell
sc query Nikita
```

Ожидаемый вывод:
```
STATE              : 4  RUNNING
```

### 2. Проверка веб-панели

Откройте в браузере: `http://localhost:8984/`

Вы должны увидеть:
- 🟢 Зелёный индикатор ClickHouse (если включен)
- Список обнаруженных баз 1С
- Uptime службы

### 3. Проверка JSON API

```bash
curl http://localhost:8984/stats_api | jq
```

Ожидаемый ответ:
```json
{
  "uptime_seconds": 120,
  "clickhouse": {
    "enabled": true,
    "connection_ok": true
  },
  "databases": [...]
}
```

### 4. Проверка логов

**Linux:**
```bash
sudo journalctl -u Nikita -n 50
```

**Windows:**
```powershell
Get-Content "debug\Nikita.*.log" -Tail 50
```

Ищите сообщения:
- ✓ Успешные операции
- Отсутствие критических ошибок (✗)

---

## Обновление

### Linux

```bash
# Остановите службу
sudo systemctl stop Nikita

# Сделайте резервную копию конфигурации
sudo cp /opt/Nikita/.env /opt/Nikita/.env.backup

# Обновите код
cd /opt/Nikita
sudo git pull

# Обновите зависимости
sudo venv/bin/pip install -r requirements.lin --upgrade

# Запустите службу
sudo systemctl start Nikita

# Проверьте логи
sudo journalctl -u Nikita -f
```

### Windows

**Из дистрибутива:**
1. Остановите службу: `net stop Nikita`
2. Запустите новый инсталлятор
3. Выберите тот же каталог установки
4. Запустите службу: `net start Nikita`

**Из исходников:**
```powershell
# Остановите службу
python Nikita.py stop

# Обновите код
git pull

# Обновите зависимости
.\venv\Scripts\activate
pip install -r requirements.win --upgrade

# Запустите службу
python Nikita.py start
```

---

## Удаление

### Linux

```bash
# Остановите и отключите службу
sudo systemctl stop Nikita
sudo systemctl disable Nikita

# Удалите unit-файл
sudo rm /etc/systemd/system/Nikita.service
sudo systemctl daemon-reload

# Удалите каталог (опционально)
sudo rm -rf /opt/Nikita
```

### Windows

**Из дистрибутива:**
1. Откройте "Установка и удаление программ"
2. Найдите "Nikita"
3. Нажмите "Удалить"

**Из исходников:**
```powershell
# Остановите и удалите службу
python Nikita.py stop
python Nikita.py remove

# Удалите каталог вручную
```

---

## Дополнительные настройки

### Автоматический перезапуск при сбое (Linux)

Добавьте в `/etc/systemd/system/Nikita.service`:
```ini
[Service]
Restart=always
RestartSec=10s
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart Nikita
```

### Ограничение ресурсов (Linux)

Добавьте в unit-файл:
```ini
[Service]
MemoryLimit=4G
CPUQuota=200%
```

---

## Помощь

Если возникли проблемы при установке:

1. Проверьте **[Устранение неполадок](troubleshooting.md)**
2. Просмотрите логи (см. выше)
3. Создайте [Issue на GitHub](https://github.com/your-org/Nikita/issues)

---

**Обновлено:** 2025-12-11  
**Версия:** 2.0.0

