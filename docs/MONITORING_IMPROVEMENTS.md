# Улучшения мониторинга и логирования Nikita

## Дата: 2025-12-05

## Обзор изменений

В проект добавлена расширенная система мониторинга и логирования для отслеживания работы службы индексации журналов регистрации 1С.

---

## 1. Расширенное логирование

### 1.1 Логирование отправки в ClickHouse (`src/parser.py::send_to_clickhouse`)

**Добавлено:**
- ⏱ Измерение времени выполнения запросов
- 📊 Вывод скорости отправки (записей/сек)
- ✓ Детальная информация о результате выполнения запроса
- ✗ Полный traceback при ошибках
- 📈 Счётчик общего количества отправленных записей за сессию

**Пример логов:**
```
→ CLICKHOUSE: Начинаем отправку пакета для базы 'TestBase' (записей: 200)
✓ CLICKHOUSE: Успешно отправлено 200 записей в таблицу zhr1c.TestBase
✓ CLICKHOUSE: Время выполнения: 0.245 сек (816.3 записей/сек)
✓ CLICKHOUSE: Всего отправлено за сессию: 1096 записей
```

### 1.2 Логирование отправки в Solr (`src/parser.py::send_to_solr`)

**Добавлено:**
- ⏱ Измерение времени выполнения
- 📊 Вывод скорости отправки
- 📈 Счётчики успешных и неудачных отправок
- ✗ Детальное логирование ошибок HTTP

### 1.3 Улучшение диспетчера отправки (`src/parser.py::post_query`)

**Добавлено:**
- 🔍 Пошаговое логирование каждой стадии отправки
- ✓/✗ Явные отметки успешности каждой операции
- 📊 Итоговая статистика в конце каждой отправки
- 🔄 Детальное логирование работы с Redis

---

## 2. Система статистики (`src/globals.py::stats`)

Новый класс `g.stats` хранит статистику работы службы в реальном времени.

### 2.1 Статистика ClickHouse
```python
g.stats.clickhouse_total_sent           # Всего отправлено записей
g.stats.clickhouse_total_errors         # Всего ошибок
g.stats.clickhouse_last_success_time    # Время последней успешной отправки
g.stats.clickhouse_last_error_time      # Время последней ошибки
g.stats.clickhouse_last_error_msg       # Текст последней ошибки
g.stats.clickhouse_connection_ok        # Статус подключения (True/False)
```

### 2.2 Статистика Solr
Аналогичные поля с префиксом `solr_*`

### 2.3 Статистика Redis
Аналогичные поля с префиксом `redis_*`, плюс:
```python
g.stats.redis_total_queued              # Всего добавлено в очередь
```

### 2.4 Общая статистика
```python
g.stats.start_time                      # Время запуска службы
g.stats.total_records_parsed            # Всего распарсено записей
g.stats.last_errors                     # Список последних 10 ошибок
                                        # Формат: [(время, тип, сообщение), ...]
```

---

## 3. Улучшенная веб-панель (`src/cherry.py::index`)

### 3.1 Новый дизайн
- 🎨 Современный Material Design интерфейс
- 📱 Адаптивная вёрстка
- 🔄 Автообновление страницы каждые 30 секунд

### 3.2 Блок статистики отправок

**Отображается:**
- ⏱ Время работы службы (uptime)
- 🟢/🔴 Статус подключения к каждому сервису
- 📊 Количество отправленных записей и ошибок
- 🕐 Время последней успешной отправки
- ⚠ Последние ошибки (с временными метками)

**Статус для каждого сервиса:**
- ClickHouse: хост, порт, БД, статистика
- Solr: хост, порт, статистика
- Redis: хост, порт, статистика очереди

### 3.3 Последние ошибки
Блок "🚨 Последние ошибки" показывает последние 5 ошибок с:
- Временем возникновения
- Типом сервиса (ClickHouse/Solr/Redis)
- Кратким описанием ошибки

---

## 4. JSON API для статистики (`src/cherry.py::stats_api`)

### 4.1 Endpoint
```
GET http://localhost:8984/stats_api
```

### 4.2 Формат ответа
```json
{
  "uptime_seconds": 3661,
  "uptime_formatted": "1ч 1м 1с",
  "total_records_parsed": 0,
  "clickhouse": {
    "enabled": true,
    "host": "localhost",
    "port": 9000,
    "database": "zhr1c",
    "connection_ok": true,
    "total_sent": 1096,
    "total_errors": 0,
    "last_success_time": "2025-12-05T11:53:11.281697",
    "last_error_time": null,
    "last_error_msg": ""
  },
  "solr": {
    "enabled": false
  },
  "redis": {
    "enabled": false
  },
  "last_errors": [
    {
      "time": "2025-12-05T11:45:20.123456",
      "type": "ClickHouse",
      "message": "Connection timeout"
    }
  ],
  "databases": [
    {
      "name": "TestBase",
      "path": "/path/to/journal",
      "format": "lgd",
      "total_size": 1000000,
      "parsed_size": 856432,
      "percent": 85.6432
    }
  ]
}
```

### 4.3 Применение
- Интеграция с внешними системами мониторинга (Prometheus, Grafana, Zabbix)
- Автоматизированные проверки состояния
- Построение дашбордов

---

## 5. Инициализация статистики (`Nikita.py::start_all`)

При запуске службы автоматически инициализируется:
```python
g.stats.start_time = datetime.now()
```

Это позволяет отслеживать uptime службы.

---

## 6. Использование

### 6.1 Просмотр веб-панели
Откройте браузер и перейдите по адресу:
```
http://localhost:8984/
```

Или укажите хост/порт из конфигурации:
```
http://<HTTP_LISTEN_INTERFACE>:<HTTP_LISTEN_PORT>/
```

### 6.2 Получение JSON статистики
```bash
curl http://localhost:8984/stats_api
```

Или с форматированием:
```bash
curl http://localhost:8984/stats_api | python -m json.tool
```

### 6.3 Мониторинг логов
Логи теперь содержат детальную информацию о каждой операции отправки:
```bash
tail -f /opt/Nikita/debug/Nikita.*.log
```

---

## 7. Примеры интеграций

### 7.1 Prometheus Exporter
```python
from prometheus_client import Gauge, start_http_server
import requests
import time

# Создаём метрики
clickhouse_sent = Gauge('nikita_clickhouse_sent', 'ClickHouse records sent')
clickhouse_errors = Gauge('nikita_clickhouse_errors', 'ClickHouse errors')

while True:
    stats = requests.get('http://localhost:8984/stats_api').json()
    clickhouse_sent.set(stats['clickhouse']['total_sent'])
    clickhouse_errors.set(stats['clickhouse']['total_errors'])
    time.sleep(15)
```

### 7.2 Zabbix агент
```bash
#!/bin/bash
# /etc/zabbix/scripts/nikita_stats.sh

case "$1" in
    clickhouse.sent)
        curl -s http://localhost:8984/stats_api | jq '.clickhouse.total_sent'
        ;;
    clickhouse.errors)
        curl -s http://localhost:8984/stats_api | jq '.clickhouse.total_errors'
        ;;
    uptime)
        curl -s http://localhost:8984/stats_api | jq '.uptime_seconds'
        ;;
esac
```

---

## 8. Преимущества

✅ **Прозрачность**: Полная видимость всех операций отправки  
✅ **Отладка**: Детальные логи с traceback для быстрого поиска проблем  
✅ **Мониторинг**: Веб-панель и JSON API для интеграции с системами мониторинга  
✅ **Производительность**: Метрики времени выполнения и скорости обработки  
✅ **История**: Хранение последних ошибок для анализа  
✅ **Диагностика**: Явные индикаторы статуса подключения к сервисам  

---

## 9. Обратная совместимость

Все изменения полностью обратно совместимы:
- Старый функционал не затронут
- Новые поля в `g.stats` инициализируются значениями по умолчанию
- Веб-интерфейс работает независимо от конфигурации сервисов
- При отсутствии данных выводится "Нет данных" вместо ошибок

---

## 10. Рекомендации

1. **Регулярно проверяйте веб-панель** для контроля состояния службы
2. **Настройте автоматический мониторинг** через JSON API
3. **Анализируйте логи** при появлении ошибок в блоке "Последние ошибки"
4. **Следите за метриками производительности** (записей/сек) для оптимизации

---

*Создано: 2025-12-05*  
*Автор: Nikita Service Development Team*
