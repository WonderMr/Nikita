# Быстрый старт: Мониторинг Nikita

Этот документ поможет вам быстро начать работу с системой мониторинга службы Nikita.

---

## 📊 Веб-панель мониторинга

### Доступ к панели

После запуска службы откройте браузер и перейдите:

```
http://localhost:8984/
```

Если вы изменили порт в `.env` (параметр `HTTP_LISTEN_PORT`), используйте свой порт.

### Что вы увидите

#### 1. Заголовок и информация о системе
- **Название службы**: Nikita / Nikita
- **Время работы (Uptime)**: Сколько времени служба работает с момента запуска
- **Время последней активности**: Когда последний раз происходила отправка данных

#### 2. Статистика сервисов (таблица)

Отображается в виде таблицы с колонками:
- **ClickHouse** / **Solr** / **Redis**: Статус подключения
  - 🟢 **Зелёный** = сервис работает нормально
  - 🔴 **Красный** = есть проблемы с подключением
  - **Отключено** = сервис выключен в конфигурации
- **Хост**: Адрес и порт подключения
- **БД**: Имя базы данных (для ClickHouse)
- **Записей**: Количество отправленных записей с момента запуска службы
- **Ошибок**: Количество ошибок отправки

#### 3. Блок "🚨 Последние ошибки"

Показывает последние 5 ошибок с:
- Временем возникновения
- Типом сервиса (ClickHouse/Solr/Redis)
- Кратким описанием ошибки

Если ошибок нет — отображается сообщение "Нет ошибок".

#### 4. Список обрабатываемых баз 1С

Таблица всех баз данных 1С, которые отслеживает служба:
- **Имя базы**
- **Путь к журналу**
- **Формат** (lgf/lgd)
- **Размер** (общий размер журналов)
- **Обработано** (сколько уже распарсено)
- **Прогресс** (процент обработки)

---

## 📈 JSON API для мониторинга

### Получить статистику в формате JSON

```bash
curl http://localhost:8984/stats_api
```

### Формат ответа

```json
{
  "uptime_seconds": 5461,
  "uptime_formatted": "1ч 31м 1с",
  "total_records_parsed": 0,
  "clickhouse": {
    "enabled": true,
    "host": "localhost",
    "port": 9000,
    "database": "zhr1c",
    "connection_ok": true,
    "total_sent": 1096,
    "total_errors": 0,
    "last_success_time": "2025-12-05T11:53:11.281697",
    "last_error_time": null,
    "last_error_msg": ""
  },
  "solr": {
    "enabled": false
  },
  "redis": {
    "enabled": false
  },
  "last_errors": [],
  "databases": [
    {
      "name": "PROD_ZUP",
      "path": "/home/usr1cv8/.1cv8/1C/1cv8/reg_1541/uuid/1Cv8Log",
      "format": "lgd",
      "total_size": 1048576,
      "parsed_size": 890000,
      "percent": 84.88
    }
  ]
}
```

### Применение JSON API

- ✅ Интеграция с **Prometheus** (см. примеры ниже)
- ✅ Интеграция с **Zabbix** (см. примеры ниже)
- ✅ Интеграция с **Grafana**
- ✅ Автоматизированные проверки состояния
- ✅ Построение собственных дашбордов

---

## 📝 Улучшенные логи

### Что нового в логах

Теперь в логах вы видите:
- ✓ **Успешные операции** с временем выполнения и скоростью обработки
- ✗ **Ошибки** с полным traceback для отладки
- 📊 **Скорость обработки** (записей/сек)
- 📈 **Счётчики** за всю сессию (общее количество отправленных записей)
- 🔄 **Детальное логирование** каждой стадии отправки

### Пример логов

```
2025-12-05 11:53:11.242833:::lgp parser:::→ CLICKHOUSE: Начинаем отправку пакета для базы 'PROD_ZUP' (записей: 200)
2025-12-05 11:53:11.281697:::lgp parser:::✓ CLICKHOUSE: Успешно отправлено 200 записей в таблицу zhr1c.PROD_ZUP
2025-12-05 11:53:11.281697:::lgp parser:::✓ CLICKHOUSE: Время выполнения: 0.016 сек (12451.5 записей/сек)
2025-12-05 11:53:11.281697:::lgp parser:::✓ CLICKHOUSE: Всего отправлено за сессию: 1096 записей
```

### Просмотр логов

**Все логи**:
```bash
tail -f /opt/Nikita/debug/Nikita.*.log
```

**Только ошибки**:
```bash
tail -f /opt/Nikita/debug/Nikita.*.log | grep "✗"
```

**Только ClickHouse**:
```bash
tail -f /opt/Nikita/debug/Nikita.*.log | grep "CLICKHOUSE"
```

---

## 🔍 Как быстро найти проблему

### 1. Проверьте веб-панель

Откройте `http://localhost:8984/` и посмотрите:
- Есть ли красные 🔴 индикаторы в таблице статистики?
- Есть ли ошибки в блоке "🚨 Последние ошибки"?
- Все ли базы 1С обрабатываются (есть ли прогресс)?

### 2. Проверьте статус подключения через JSON API

```bash
curl -s http://localhost:8984/stats_api | jq '.clickhouse.connection_ok'
# Должно вернуть: true

curl -s http://localhost:8984/stats_api | jq '.clickhouse.total_errors'
# Должно вернуть: 0 (или небольшое число)
```

### 3. Проверьте логи на наличие ошибок

```bash
tail -100 /opt/Nikita/debug/Nikita.*.log | grep "✗"
```

### 4. Проверьте работу ClickHouse (если включен)

```bash
# Проверка статуса
systemctl status clickhouse-server

# Проверка подключения
clickhouse-client --query "SELECT 1"

# Проверка баз данных
clickhouse-client --query "SHOW DATABASES"
```

---

## ⚙️ Интеграция с системами мониторинга

### Prometheus

Создайте простой экспортер для Prometheus:

```python
#!/usr/bin/env python3
import requests
import time
from prometheus_client import Gauge, start_http_server

# Создаём метрики
clickhouse_sent     = Gauge('nikita_clickhouse_sent', 'ClickHouse records sent')
clickhouse_errors   = Gauge('nikita_clickhouse_errors', 'ClickHouse errors')
connection_ok       = Gauge('nikita_connection_ok', 'Connection status', ['service'])

# Запускаем HTTP-сервер Prometheus на порту 9100
start_http_server(9100)

while True:
    try:
        stats = requests.get('http://localhost:8984/stats_api', timeout=5).json()
        
        # Обновляем метрики
        if stats.get('clickhouse', {}).get('enabled'):
            clickhouse_sent.set(stats['clickhouse']['total_sent'])
            clickhouse_errors.set(stats['clickhouse']['total_errors'])
            connection_ok.labels(service='clickhouse').set(
                1 if stats['clickhouse']['connection_ok'] else 0
            )
        
        time.sleep(15)  # Обновление каждые 15 секунд
    except Exception as e:
        print(f"Ошибка: {e}")
        time.sleep(15)
```

Добавьте в `prometheus.yml`:
```yaml
scrape_configs:
  - job_name: 'nikita'
    static_configs:
      - targets: ['localhost:9100']
```

### Zabbix

Создайте скрипт `/etc/zabbix/scripts/nikita_stats.sh`:

```bash
#!/bin/bash
# Скрипт для получения статистики Nikita

case "$1" in
    clickhouse.sent)
        curl -s http://localhost:8984/stats_api | jq '.clickhouse.total_sent'
        ;;
    clickhouse.errors)
        curl -s http://localhost:8984/stats_api | jq '.clickhouse.total_errors'
        ;;
    clickhouse.connection)
        curl -s http://localhost:8984/stats_api | jq '.clickhouse.connection_ok' | sed 's/true/1/;s/false/0/'
        ;;
    uptime)
        curl -s http://localhost:8984/stats_api | jq '.uptime_seconds'
        ;;
    *)
        echo "Использование: $0 {clickhouse.sent|clickhouse.errors|clickhouse.connection|uptime}"
        exit 1
        ;;
esac
```

Сделайте скрипт исполняемым:
```bash
chmod +x /etc/zabbix/scripts/nikita_stats.sh
```

В Zabbix создайте элементы данных (Items):
- **Ключ**: `nikita.clickhouse.sent`
- **Тип**: Внешняя проверка
- **Команда**: `/etc/zabbix/scripts/nikita_stats.sh clickhouse.sent`

### Grafana

Используйте Prometheus в качестве источника данных, создайте дашборд с панелями:
- **Graph**: `nikita_clickhouse_sent` (записей отправлено)
- **Singlestat**: `nikita_connection_ok{service="clickhouse"}` (статус подключения)
- **Graph**: `rate(nikita_clickhouse_sent[5m])` (скорость отправки записей/сек)

---

## 📖 Дополнительная информация

Для подробного описания всех улучшений см. [MONITORING_IMPROVEMENTS.md](MONITORING_IMPROVEMENTS.md)

---

*Обновлено: 2025-12-05*
