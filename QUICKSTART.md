# Быстрый старт Nikita

Этот документ поможет вам запустить службу Nikita за 5-10 минут на Linux или Windows.

---

## Linux (установка за 6 команд)

### Предварительные требования

- Python 3.10+ установлен
- ClickHouse запущен (опционально, но рекомендуется)
- Права `sudo` для установки службы

### Установка

```bash
# 1. Клонирование репозитория в /opt/Nikita
sudo git clone https://github.com/your-org/Nikita.git /opt/Nikita
cd /opt/Nikita

# 2. Создание виртуального окружения и установка зависимостей
sudo python3 -m venv venv
sudo venv/bin/pip install -r requirements.lin

# 3. Настройка прав доступа (ВАЖНО!)
# Передаем владение папкой пользователю 1С (обычно usr1cv8:grp1cv8)
sudo chown -R usr1cv8:grp1cv8 /opt/Nikita

# 4. Копирование и настройка конфигурации
sudo cp env.example .env
sudo nano .env  # Отредактируйте: укажите пути к базам 1С, настройки ClickHouse

# 5. Установка systemd service через симлинк
sudo ln -s /opt/Nikita/Nikita.service /etc/systemd/system/Nikita.service
sudo systemctl daemon-reload

# 6. Запуск службы
sudo systemctl enable Nikita
sudo systemctl start Nikita
```

### Проверка работы

```bash
# Статус службы
sudo systemctl status Nikita

# Логи в реальном времени
sudo journalctl -u Nikita -f

# Веб-панель мониторинга
# Откройте в браузере: http://your-server:8984/
```

---

## Windows (установка за 3 шага)

### Вариант A: Установка из дистрибутива (рекомендуется)

1. **Скачайте инсталлятор** `Nikita.setup.ДАТА.exe` из [releases](https://github.com/your-org/Nikita/releases)

2. **Запустите инсталлятор** от имени Администратора
   - Выберите каталог установки (по умолчанию: `C:\Program Files\Nikita`)
   - Инсталлятор автоматически установит все зависимости

3. **Настройте конфигурацию**
   ```cmd
   notepad "C:\Program Files\Nikita\.env"
   ```
   - Укажите пути к базам 1С (`C1_SRVINFO_PATH` или `IBASE_0_JR`)
   - Настройте подключение к ClickHouse

4. **Запустите службу**
   ```powershell
   # Откройте PowerShell от Администратора
   cd "C:\Program Files\Nikita"
   .\Nikita.exe start
   ```

5. **Проверка работы**
   - Откройте `http://localhost:8984/` в браузере
   - Или проверьте статус: `sc query Nikita`

### Вариант B: Установка из исходников

**Предварительные требования:**
- Python 3.10+ установлен и добавлен в PATH
- Git для клонирования репозитория

```powershell
# 1. Клонирование репозитория
git clone https://github.com/your-org/Nikita.git
cd Nikita

# 2. Создание виртуального окружения
python -m venv venv
.\venv\Scripts\activate

# 3. Установка зависимостей
pip install -r requirements.win

# 4. Настройка конфигурации
copy env.example .env
notepad .env  # Отредактируйте параметры

# 5. Установка и запуск службы (от Администратора)
python Nikita.py install
python Nikita.py start
```

**Проверка работы:**
```powershell
# Статус службы
sc query Nikita

# Или откройте Services.msc и найдите "Nikita"

# Веб-панель
# http://localhost:8984/
```

---

## Минимальная конфигурация

Отредактируйте файл `.env` и укажите минимально необходимые параметры:

```ini
# 1. Путь к базам 1С (автодетект)
C1_SRVINFO_PATH=/home/usr1cv8/.1cv8/1C/1cv8

# Или укажите базу вручную
IBASE_0=МояБаза
IBASE_0_JR=/путь/к/1Cv8Log
IBASE_0_FORMAT=lgd

# 2. ClickHouse (включен по умолчанию)
CLICKHOUSE_ENABLED=true
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_DATABASE=zhr1c

# 3. Веб-сервер мониторинга
HTTP_LISTEN_INTERFACE=0.0.0.0
HTTP_LISTEN_PORT=8984
```

**Примечание:** База данных и таблицы в ClickHouse создаются автоматически при первом запуске.

---

## Управление службой

### Linux (systemd)

```bash
# Статус
sudo systemctl status Nikita

# Запуск
sudo systemctl start Nikita

# Остановка
sudo systemctl stop Nikita

# Перезапуск
sudo systemctl restart Nikita

# Просмотр логов
sudo journalctl -u Nikita -f

# Последние 100 строк логов
sudo journalctl -u Nikita -n 100

# Отключение автозапуска
sudo systemctl disable Nikita
```

### Windows (Service)

```powershell
# Запуск
python Nikita.py start
# или
net start Nikita

# Остановка
python Nikita.py stop
# или
net stop Nikita

# Перезапуск
python Nikita.py restart

# Удаление службы
python Nikita.py remove

# Просмотр логов
# Логи находятся в: C:\путь\к\Nikita\debug\Nikita.*.log
```

---

## Проверка работоспособности

### 1. Проверка веб-панели

Откройте в браузере: `http://localhost:8984/`

**Что должно быть видно:**
- 🟢 Зелёный индикатор подключения к ClickHouse
- Список обнаруженных баз 1С
- Счётчики отправленных записей

### 2. Проверка через JSON API

```bash
# Linux
curl http://localhost:8984/stats_api | jq

# Windows (PowerShell)
Invoke-RestMethod http://localhost:8984/stats_api | ConvertTo-Json
```

**Ожидаемый ответ:**
```json
{
  "uptime_seconds": 120,
  "clickhouse": {
    "enabled": true,
    "connection_ok": true,
    "total_sent": 0
  },
  "databases": [
    {
      "name": "База1",
      "format": "lgd",
      "percent": 0.0
    }
  ]
}
```

### 3. Проверка логов

**Linux:**
```bash
sudo journalctl -u Nikita -n 50
```

**Windows:**
```powershell
Get-Content "debug\Nikita.*.log" -Tail 50
```

**Что искать:**
- ✓ Успешные операции (`✓ CLICKHOUSE: Успешно отправлено`)
- Отсутствие критических ошибок (`✗`)

---

## Устранение типичных проблем

### Проблема: Служба не запускается

**Linux:**
```bash
# Смотрим подробные логи
sudo journalctl -u Nikita -n 50 --no-pager

# Проверяем конфигурацию в консольном режиме
/opt/Nikita/venv/bin/python /opt/Nikita/Nikita.py console
```

**Windows:**
```powershell
# Запускаем в консольном режиме для диагностики
python Nikita.py console
```

### Проблема: Не найдены базы 1С

**Решение:**
1. Проверьте путь `C1_SRVINFO_PATH` в `.env`
2. Или укажите базу вручную через `IBASE_0`, `IBASE_0_JR`
3. Проверьте права доступа к каталогам журналов

**Linux:**
```bash
# Проверьте права
ls -la /home/usr1cv8/.1cv8/1C/1cv8/

# Добавьте пользователя в группу usr1cv8
sudo usermod -a -G usr1cv8 $(whoami)
```

### Проблема: ClickHouse недоступен

**Решение:**
```bash
# Linux: Проверьте статус ClickHouse
sudo systemctl status clickhouse-server

# Проверьте подключение
clickhouse-client --query "SELECT 1"

# Windows: Проверьте, запущен ли ClickHouse
# (если установлен локально)
```

Если ClickHouse не установлен — установите его или отключите в `.env`:
```ini
CLICKHOUSE_ENABLED=false
```

### Проблема: Права доступа (Exit Code 77)

**Linux:**
```bash
# Убедитесь, что владелец папки — пользователь службы
sudo chown -R usr1cv8:grp1cv8 /opt/Nikita

# Проверьте права на запись
sudo -u usr1cv8 touch /opt/Nikita/test.txt
```

### Проблема: Не открывается веб-панель

**Решение:**
1. Проверьте, что служба запущена
2. Проверьте порт в `.env` (`HTTP_LISTEN_PORT=8984`)
3. Проверьте firewall:

**Linux:**
```bash
sudo ss -tlnp | grep 8984
sudo firewall-cmd --add-port=8984/tcp --permanent  # CentOS/RHEL
sudo ufw allow 8984  # Ubuntu/Debian
```

**Windows:**
```powershell
# Выполните от Администратора
netsh advfirewall firewall add rule name="Nikita Web" dir=in action=allow protocol=TCP localport=8984
```

---

## Следующие шаги

После успешной установки:

1. **Изучите веб-панель** — `http://localhost:8984/`
2. **Настройте мониторинг** — см. [docs/monitoring.md](docs/monitoring.md)
3. **Оптимизируйте конфигурацию** — см. [docs/configuration.md](docs/configuration.md)

---

## Дополнительные ресурсы

- **[Полная документация](docs/README.md)** — индекс всей документации
- **[Установка (детально)](docs/installation.md)** — подробное руководство
- **[Конфигурация](docs/configuration.md)** — все параметры `.env`
- **[Устранение неполадок](docs/troubleshooting.md)** — FAQ и решения проблем

---

**Обновлено:** 2025-12-11  
**Версия:** 2.0.0
