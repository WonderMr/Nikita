# 🔧 CHANGELOG - Исправления после аудита кода

**Дата:** 2025-12-09  
**Версия:** dev branch  

---

## 🟢 Оптимизация и улучшения

### 10. Оптимизация хранения SQLite - составной ключ (database + basename) вместо полных путей
- **Дата:** 2025-12-09
- **Файлы:** `src/state_manager.py`, `src/parser.py`
- **Проблема:** 
  - В SQLite базе `Nikita.parser.state.db` хранились полные пути к файлам (например, `/long/path/to/base1/20251209.lgp`), что было избыточно
  - Использование только `basename` создавало **коллизии** — файлы с одинаковыми именами из разных баз перезаписывали друг друга
- **Решение:**
  - **Изменена структура таблиц:**
    - `file_states`: PRIMARY KEY теперь `(database_name, file_basename)` вместо `filename`
    - `committed_blocks`: поля `database_name` (имя базы 1С) + `file_basename` (имя файла) вместо `filename` + `basename`
  - **Обновлены методы:**
    - `get_file_state(filename, database_name)` — добавлен параметр `database_name`
    - `update_file_state(filename, filesize, filesizeread, database_name)` — добавлен параметр `database_name`
    - `log_committed_block(filename, offset_start, offset_end, data_records, database_name)` — параметр `basename` переименован в `database_name`
    - `get_total_records_sent(database_name)` — упрощён поиск по `database_name`
  - **Автоматическая миграция:** При инициализации StateManager автоматически мигрирует старую структуру в новую (встроено в `_init_db()`)
  - **Обновлены вызовы в `parser.py`:** Все вызовы теперь передают `pf_base` (имя базы) как `database_name`
- **API изменён (breaking change):** Добавлен обязательный параметр `database_name` в методы StateManager
- **Преимущества:**
  - ✅ Уменьшен размер базы данных (хранится basename вместо полного пути)
  - ✅ **Устранены коллизии** — файлы из разных баз больше не перезаписывают друг друга
  - ✅ Улучшена читаемость при отладке
  - ✅ Корректная работа с несколькими базами 1С

---

## 🔴 Критические исправления

### 1. Исправлена опечатка `os._exists()` → `os.path.exists()`
- **Файл:** `Nikita.py:359`
- **Проблема:** Вызов несуществующего метода `os._exists()` приводил к AttributeError
- **Решение:** Заменено на корректный `os.path.exists()`

### 2. Исправлена утечка ресурсов SQLite
- **Файл:** `src/tools.py:116-132`
- **Проблема:** Закрывался только cursor, но не connection, что приводило к утечке дескрипторов файлов
- **Решение:** Добавлено правильное закрытие connection в блоке finally

### 3. Исправлена SQL Injection уязвимость (с вашей помощью)
- **Файлы:** `src/tools.py`, `src/parser.py`, `src/reader.py`
- **Проблема:** Выполнение SQL запросов через форматирование строк
- **Решение:**
  - `sqlite3_exec` обновлена для поддержки параметров (`params=None`)
  - `parser.py`: Запрос выборки событий переведен на параметры `?`
  - `reader.py`: Запрос выборки по ID (`IN (...)`) переведен на генерацию плейсхолдеров

### 4. Добавлена потокобезопасность для g.parser.ibases
- **Файлы:** `src/globals.py`, `Nikita.py`, `src/parser.py`, `src/reader.py`, `src/cherry.py`
- **Проблема:** Список g.parser.ibases модифицировался из разных потоков без синхронизации (race condition)
- **Решение:** 
  - Добавлен `threading.Lock()` в `src/globals.py`
  - Все операции чтения/записи g.parser.ibases обернуты в `with g.ibases_lock:`

### 5. Устранен race condition в parser
- **Файл:** `src/parser.py:499-500`
- **Проблема:** Использование `self.i_` как временной переменной в цикле небезопасно в многопоточной среде
- **Решение:** Убрано использование атрибута экземпляра, прямое присваивание `rslt[l_i] = rec[l_i]`

### 6. Исправлено неправильное использование str.find()
- **Файл:** `Nikita.py:424`
- **Проблема:** `find() > 0` не находит подстроку в начале строки (возвращает 0)
- **Решение:** Заменено на `in` оператор для корректной проверки вхождения

### 7. Изменен небезопасный дефолт для ClickHouse
- **Файл:** `src/globals.py:92`
- **Проблема:** ClickHouse включен по умолчанию (`'True'`), что небезопасно без явной настройки
- **Решение:** Изменено на `'False'` - требуется явное включение в .env

### 8. Исправлено несоответствие типов
- **Файл:** `src/reader.py:161`
- **Проблема:** Возвращался int `0` вместо str `'0'`
- **Решение:** Изменено на `'0'` для соответствия строковому типу

---

## ⚡ Оптимизации производительности

### 9. Оптимизировано чтение больших лог-файлов
- **Файл:** `src/cherry.py:1009-1021`
- **Проблема:** `readlines()` загружал весь файл в память, что критично для файлов >100MB
- **Решение:** Использование `collections.deque(f, maxlen=N)` для эффективного чтения последних N строк

### 10. Убраны избыточные вызовы read_ib_dictionary
- **Файл:** `src/parser.py:585-587`
- **Проблема:** Словарь перечитывался на каждой итерации цикла обработки файла
- **Решение:** Перемещен вызов вне основного цикла while

---

## 🛡️ Улучшения безопасности

### 11. Добавлена валидация пользовательского ввода
- **Файл:** `src/cherry.py:964-974`
- **Проблема:** Отсутствовала валидация filter_text, риск ReDoS атак
- **Решение:** 
  - Ограничение длины filter_text до 1000 символов
  - Улучшена обработка ошибок компиляции regex

### 12. Потокобезопасное логирование и типизация
- **Файл:** `src/tools.py`
- **Проблема:** Запись в лог из нескольких потоков могла портить файл; отсутствовали подсказки типов
- **Решение:** 
  - Добавлен `threading.Lock` для записи логов (`debug_print`, `log_msg`)
  - Добавлены type hints к публичным методам
- **Файл:** `src/sender.py`
- **Проблема:** Не было type hints и валидации типов аргументов отправки
- **Решение:** Добавлены аннотации для ClickHouse/Solr отправок

---

## ✅ Итоговая статистика

| Тип изменения | Количество | Приоритет |
|---------------|------------|-----------|
| Критические баги | 8 | 🔴 Высокий |
| Оптимизации | 2 | ⚡ Средний |
| Безопасность | 2 | 🛡️ Высокий |
| Улучшения кода | 2 | 📝 Низкий |
| **ВСЕГО** | **14** | - |

---

## ⚠️ Важные замечания

1. **Безопасность**: После применения изменений требуется явно включить ClickHouse в .env: `CLICKHOUSE_ENABLED=True`
2. **SQL**: Теперь все запросы к SQLite выполняются с параметризацией, что предотвращает SQL Injection.

---

**Автор исправлений:** AI Code Reviewer + User  
**Статус:** 🟢 Полностью исправлено
