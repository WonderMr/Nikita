# Сборка Nikita Windows Distribution

## Быстрый старт

### Автоматическая сборка дистрибутива и инсталлятора

```cmd
.\scripts\!c.build.win.distr.cmd
```

Этот скрипт **последовательно выполняет 3 этапа**:

### 📦 Этап 1: Подготовка зависимостей
- ✅ Скачивает **Java JDK 17** (если отсутствует)
- ✅ Скачивает **Apache Solr** (если отсутствует)
- ✅ **Если ошибка** — выдаётся предупреждение, процесс продолжается

### 🔨 Этап 2: Сборка приложения
- ✅ Останавливает и удаляет существующий сервис (если есть)
- ✅ Собирает приложение с PyInstaller (с оптимизацией)
- ✅ Копирует DLL файлы
- ✅ **Если ошибка** — процесс останавливается

### 🎁 Этап 3: Создание инсталлятора
- ✅ Копирует Java и Solr в дистрибутив
- ✅ **Автоматически устанавливает NSIS** (через Chocolatey или скачивает)
- ✅ Создаёт Windows инсталлятор (.exe)
- ✅ **Если ошибка** — процесс останавливается

### Результаты сборки

После успешного выполнения:
- **Дистрибутив**: `dist\Nikita\` — готовая портативная версия
- **Инсталлятор**: `scripts\Nikita.setup.ДАТА.exe` — Windows установщик (~175 MB)

---

## Порядок выполнения (важно!)

Скрипты работают **строго последовательно**:

```
1. ПОДГОТОВКА ЗАВИСИМОСТЕЙ
   ├── Загрузка Java (если нет)
   ├── Загрузка Solr (если нет)
   └── ❌ При ошибке: WARNING, продолжаем

2. СБОРКА ПРИЛОЖЕНИЯ
   ├── Очистка старых сборок
   ├── Переименование .yp → .py
   ├── PyInstaller
   ├── Копирование DLL
   └── ❌ При ошибке: STOP

3. СОЗДАНИЕ ИНСТАЛЛЯТОРА
   ├── Проверка дистрибутива
   ├── Установка NSIS (если нет)
   ├── Копирование Java/Solr в dist
   ├── Создание .exe
   └── ❌ При ошибке: STOP
```

**Преимущества такого порядка:**
- 🚀 Все зависимости скачиваются **до** начала сборки
- ⏱️ Если что-то не скачалось — узнаем **сразу**
- 🛡️ Сборка не начнётся, пока всё не готово
- 💾 Экономия времени при повторных сборках

### 1. Только сборка приложения

```powershell
.\scripts\build.ps1 -Optimize -NoTest
```

**Параметры:**
- `-Optimize` — оптимизированная сборка (рекомендуется)
- `-NoTest` — пропустить тестирование собранного .exe

**Результат:** `dist\Nikita\Nikita.exe`

### 2. Только создание инсталлятора

```powershell
.\scripts\create-installer.ps1
```

**Требования:**
- Уже должен быть собран дистрибутив (`dist\Nikita\Nikita.exe`)
- NSIS будет установлен автоматически, если отсутствует

**Результат:** `scripts\Nikita.setup.ДАТА.exe`

---

## Автоматическая загрузка зависимостей

### Java (JDK 17)

**Умная проверка:**
- ✅ Если папка `java\` отсутствует → автоматическая загрузка
- ✅ Если папка `java\` **пустая** → удаление и загрузка
- ✅ Если папка `java\` содержит файлы → используется существующая

**Что скачивается:**
- Автоматически скачивается OpenJDK 17 с GitHub (Adoptium Temurin)
- Размер: ~180 MB
- Распаковывается в `java\jdk-17.0.12+7\`
- Копируется в дистрибутив автоматически

### Apache Solr

**Умная проверка:**
- ✅ Если папка `solr\` отсутствует → автоматическая загрузка
- ✅ Если папка `solr\` **пустая** → удаление и загрузка
- ✅ Если папка `solr\` содержит файлы → используется существующая

**ВАЖНО:** Если автоматическая загрузка Solr не удалась (404 ошибка):

Сборка **автоматически остановится** и покажет инструкции:

```
⛔ ТРЕБУЕТСЯ РУЧНАЯ УСТАНОВКА SOLR
❌ Автоматическая загрузка не удалась (404 Not Found)

📋 ДВА ВАРИАНТА РЕШЕНИЯ:

ВАРИАНТ 1: Установить Solr вручную
  1. Откройте: https://solr.apache.org/downloads.html
  2. Скачайте Solr 8.x или 9.x (рекомендуется: 9.6.1, 9.5.0, 8.11.3)
  3. Распакуйте в: C:\Path\To\Project\solr\
  4. Структура: solr\solr-X.X.X\bin\solr.cmd
  5. Запустите сборку снова

ВАРИАНТ 2: Собрать без Solr
  .\scripts\build.ps1 -Optimize -NoTest -SkipSolr
```

**Что происходит:**
- ✅ Попытка загрузки с 3 зеркал Apache
- ❌ Если все зеркала недоступны — **сборка останавливается**
- 📋 Показываются четкие инструкции
- 🔄 После установки — запустите сборку снова

### NSIS (Nullsoft Scriptable Install System)

Если NSIS не установлен в системе:
1. **Через Chocolatey** (если установлен):
   ```powershell
   choco install nsis -y
   ```
2. **Скачивание установщика** (если Chocolatey недоступен):
   - Загружается с SourceForge
   - Устанавливается в локальную папку `nsis\`

---

## Обработка ошибок

Скрипты **прерываются немедленно** при возникновении ошибки на любом этапе:

✅ **Проверяются:**
- Наличие Python, pip, git
- Успешность PyInstaller
- Создание исполняемого файла
- Копирование файлов
- Код возврата NSIS

❌ **При ошибке:**
- Выводится понятное сообщение
- Показывается код ошибки
- Процесс останавливается
- Возврат файлов `.py` → `.yp` (откат)

**Пример:**
```
==================================================
ERROR: Build failed with exit code 1!
==================================================
The build process encountered an error and cannot continue.
Please check the error messages above.
```

---

## Защита исходников

После успешной сборки все файлы `.py` (кроме `cherry.py` и `__init__.py`) автоматически переименовываются обратно в `.yp`:

```
src/parser.py    →  src/parser.yp
src/reader.py    →  src/reader.yp
src/globals.py   →  src/globals.yp
...
```

При откате (ошибке) — аналогично.

---

## Требования

### Обязательные

- **Python 3.8 - 3.14** (автоматически ищется в системе)
- **pip** (для установки зависимостей)
- **PyInstaller** (устанавливается через `pip install pyinstaller`)

### Опциональные

- **Git** (для некоторых операций)
- **Chocolatey** (для автоматической установки NSIS)

---

## Структура скриптов

```
scripts/
├── !c.build.win.distr.cmd      ← Главный скрипт (CMD)
├── build.ps1                   ← Сборка приложения (PowerShell)
├── create-installer.ps1        ← Создание инсталлятора (PowerShell)
├── common.ps1                  ← Общие функции
├── c.installer.nsi             ← NSIS скрипт
└── Nikita.setup.ДАТА.exe       ← Результат
```

---

## Устранение проблем

### Python не найден

```
✗ Python не найден. Установите Python 3.8-3.14
```

**Решение:**
- Установите Python с https://www.python.org/
- Убедитесь, что Python добавлен в PATH

### PyInstaller не установлен

```powershell
pip install pyinstaller
```

### NSIS не может быть установлен

Установите вручную с https://nsis.sourceforge.io/

### Solr не загружается (404)

Это **не критично** — сборка продолжится без Solr.  
Можно скачать вручную и поместить в папку `solr/`.

---

## Дополнительные команды

### Тесты

```powershell
.\scripts\run-tests.ps1
```

### Очистка

```cmd
.\scripts\__Clean.cmd
```

Удаляет:
- `dist/`
- `build/`
- `*.spec`
- `.pyd` файлы

---

## Примечания

- Сборка с оптимизацией занимает ~1-2 минуты
- Инсталлятор создаётся ~5-10 секунд
- Размер финального инсталлятора: ~175 MB (с Java и зависимостями)
- NSIS устанавливается один раз, затем переиспользуется

---

**Автор:** Nikita Build System  
**Версия:** 2025-12-11  
**Поддержка:** Python 3.8-3.14, Windows 10/11/Server

