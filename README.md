# Nikita

Высокопроизводительный сервис парсинга и экспорта журналов событий 1С:Предприятие в ClickHouse, Solr и Redis.

## 🚀 Быстрый старт

**Linux (5 команд для установки):**
```bash
sudo git clone https://github.com/your-repo/Nikita.git /opt/Nikita
cd /opt/Nikita && sudo python3 -m venv venv && sudo venv/bin/pip install -r requirments.lin
sudo cp env.example .env && sudo nano .env
sudo ln -s /opt/Nikita/Nikita.service /etc/systemd/system/Nikita.service && sudo systemctl daemon-reload
sudo systemctl enable Nikita && sudo systemctl start Nikita
```

Подробности: [QUICKSTART.md](QUICKSTART.md)

## 📖 Документация

- **[Полная документация](docs/README.md)** - установка, конфигурация, архитектура
- **[Быстрый старт](QUICKSTART.md)** - установка за 5 минут на Linux
- **[Мониторинг](docs/MONITORING_QUICKSTART.md)** - настройка веб-панели и API
- **[Скрипты](docs/scripts.md)** - вспомогательные скрипты

## ✨ Основные возможности

- ✅ **ClickHouse** - колоночное хранение для аналитики (включено по умолчанию)
- ✅ **Многопоточный парсинг** - высокая производительность
- ✅ **Автодетект баз 1С** - автоматическое обнаружение журналов
- ✅ **Веб-панель мониторинга** - визуализация статистики в реальном времени
- ✅ **Поддержка форматов** - `.lgf` (текстовый) и `.lgd` (SQLite)
- ✅ **REST API** - интеграция с Prometheus, Zabbix, Grafana
- ⚙️ **Solr** - полнотекстовый поиск (опционально)
- ⚙️ **Redis** - буферизация при недоступности хранилищ (опционально)

## 🖥 Системные требования

- Python 3.10+
- ClickHouse (рекомендуется)
- Доступ к журналам сервера 1С:Предприятие

## 📊 Веб-панель мониторинга

После установки доступна по адресу: `http://your-server:8984/`

- Статистика отправки в ClickHouse/Solr/Redis
- Список обрабатываемых баз 1С
- Процент обработки журналов
- Последние ошибки
- Отладочные сообщения

## 🔧 Управление службой (Linux)

```bash
# Статус
sudo systemctl status Nikita

# Логи
sudo journalctl -u Nikita -f

# Перезапуск
sudo systemctl restart Nikita
```

## 📝 Лицензия

Смотрите файлы в каталоге `licenses/`

## 🤝 Участие в разработке

Pull requests приветствуются! Для значительных изменений сначала откройте issue для обсуждения.

